name = "renderinvoice"
main = "src/worker.ts"
compatibility_date = "2024-11-01"
# pdf-lib + fontkit reach for node:buffer / node:stream.
compatibility_flags = ["nodejs_compat"]

[[rules]]
type = "CompiledWasm"
globs = ["**/*.wasm"]
fallthrough = true

# Set with: wrangler secret put API_KEY_SECRET
